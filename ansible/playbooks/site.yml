---
- name: Configure all app servers
  hosts: all
  gather_facts: true
  roles:
    - role: "{{ playbook_dir }}/../roles/hardening"
#    - role: "{{ playbook_dir }}/../roles/webserver"
#    - role: "{{ playbook_dir }}/../roles/flask_app"